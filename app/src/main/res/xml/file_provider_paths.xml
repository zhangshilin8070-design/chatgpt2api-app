<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- 把 internal cache 目录下的 updates/ 子目录暴露给 FileProvider，用于
         下载新版本 APK 后通过 ACTION_VIEW + content:// URI 调起安装。-->
    <cache-path
        name="updates"
        path="updates/" />
</paths>
